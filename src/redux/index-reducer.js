let initialState = {
    email: null,
    color: null,
    is_auth: false
};

export const indexReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
}

export default indexReducer;